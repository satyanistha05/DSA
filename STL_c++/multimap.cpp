#include<bits/stdc++.h>
using namespace std;
void explainMultimap(){
    multimap<int,int>mpp;
    //everything same as map
    //but it can store duplicate keys
    //but it will store them in order of value
    //only mpp[key] cannot be used here
}