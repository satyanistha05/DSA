#include<bits/stdc++.h>
using namespace std;

void explainuset(){
    unordered_set<int> st;
    //lower bound and upper bound funtion
    //does not work rest all function work
    //as above, it does not stores in any
    //particular order it has a better time complexity
    //than set in most cases,except some when collison happens

    //time complexity is o(1)-->best case
    //time complexity is o(n)-->worst case
}